# Различия между текущим кодом и целевым устройством MiiBestOD / Spotpear ESP32-S3-Touch-AMOLED-1.8

## Обзор

Документ описывает ключевые различия между текущей реализацией проекта TamaFi и целевым устройством **MiiBestOD / Spotpear ESP32-S3-Touch-AMOLED-1.8**, которые необходимо учесть при миграции.

**Примечание:** Это одно и то же устройство, продается под разными брендами (MiiBestOD, Spotpear) с разными корпусами.

---

## 1. Размеры и разрешение экрана

### Текущий код (TamaFi.ino, ui.cpp, User_Setup.h)

```cpp
// TamaFi.ino
#define TFT_W 240
#define TFT_H 240

// ui.cpp
static const int TFT_W = 240;
static const int TFT_H = 240;
static const int PET_W = 115;
static const int PET_H = 110;
static const int EFFECT_W = 100;
static const int EFFECT_H = 95;

// User_Setup.h
#define TFT_WIDTH   240
#define TFT_HEIGHT  340  // Примечание: высота указана 340, но используется 240
```

**Характеристики:**
- **Разрешение:** 240×240 пикселей
- **Тип дисплея:** TFT ST7789
- **Библиотека:** TFT_eSPI
- **Размер спрайтов:** 
  - Питомец: 115×110
  - Эффекты: 100×95
  - Фреймбуфер: 240×240

### Целевое устройство (MiiBestOD / Spotpear ESP32-S3-Touch-AMOLED-1.8)

**Характеристики:**
- **Размер дисплея:** 1.8 дюйма
- **Разрешение:** 368×448 пикселей (нативное)
- **Соотношение сторон:** ~0.82:1 (вертикальный формат, высота больше ширины)
- **Тип дисплея:** AMOLED
- **Библиотека:** Требует уточнения (возможно, не TFT_eSPI)
- **Даун-скейлинг:** Допускается использование меньших разрешений при сохранении соотношения сторон

### Стратегия адаптации разрешения

**Выбранный подход: Апскейл квадратного контента + область управления**

| Область | Разрешение | Масштаб | Расположение | Назначение |
|---------|------------|---------|--------------|------------|
| **Основной контент** | 240×240 (исходное) | 1.533× | 0,0 - 368,368 | Игровой контент, питомец, фон |
| **Область управления** | 368×80 | 1:1 | 0,368 - 368,448 | Виртуальные кнопки, статус-бары, UI |

**Расчеты:**
- Ширина экрана: 368 пикселей
- Исходная ширина контента: 240 пикселей
- Масштаб по ширине: 368/240 = 1.533×
- Высота после масштабирования: 240 × 1.533 = 368 пикселей
- Остается снизу: 448 - 368 = 80 пикселей для управления

**Альтернативные варианты (для справки):**

При сохранении соотношения сторон 368:448 (≈0.8214:1) возможны следующие варианты даун-скейлинга:

| Разрешение | Масштаб | Ширина | Высота | Соотношение | Примечание |
|------------|---------|--------|--------|-------------|------------|
| **368×448** | 100% | 368 | 448 | 0.8214:1 | Нативное разрешение |
| **276×336** | 75% | 276 | 336 | 0.8214:1 | Хороший баланс |
| **184×224** | 50% | 184 | 224 | 0.8214:1 | Половина от нативного |
| **240×292** | ~65% | 240 | 292 | 0.8219:1 | Близко к исходному 240×240 |

**Рекомендации:**
- **✅ РЕКОМЕНДУЕТСЯ: Апскейл 240×240 → 368×368 + область управления 368×80**
  - Основной контент масштабируется до 368×368 (верх экрана)
  - Нижняя область 80 пикселей для виртуальных кнопок и UI элементов
  - Минимальные изменения в коде, эффективное использование экрана
- **Для быстрого старта:** Использовать letterboxing с исходным 240×240
- **Для оптимизации:** Использовать 276×336 (75%) — хороший баланс между качеством и производительностью
- **Для максимального качества:** Использовать нативное 368×448 с полной переработкой UI

### Проблемы и решения

#### Проблема 1: Несоответствие разрешения и соотношения сторон
- **Проблема:** 
  - Код жестко завязан на разрешение 240×240 (квадрат 1:1)
  - Целевое устройство имеет разрешение 368×448 (вертикальный формат ~0.82:1)
  - Разница в соотношении сторон: квадрат → вертикальный прямоугольник
- **Влияние:** 
  - Все спрайты и UI элементы рассчитаны на квадратный экран 240×240
  - Позиционирование элементов (petPosX = 120, petPosY = 90) рассчитано на центр квадрата
  - Фоновые изображения имеют размер 240×240
  - UI будет искажен при прямом переносе на вертикальный экран
- **Решение:**
  - **Вариант А: Использовать нативное разрешение 368×448**
    - Переработать весь UI под вертикальный формат
    - Пересчитать все позиции элементов
    - Пересоздать или масштабировать спрайты под новое разрешение
    - Использовать дополнительное пространство для улучшения UI
  - **Вариант Б: Использовать даун-скейлинг с сохранением соотношения сторон**
    - Варианты даун-скейлинга: 184×224 (1/2), 276×336 (3/4), или другое
    - Преимущество: меньше изменений в коде, но теряется часть экрана
    - Центрировать контент 240×240 в доступной области или масштабировать
  - **Вариант В: Letterboxing (черные полосы)**
    - Отображать 240×240 контент по центру экрана 368×448
    - Добавить черные полосы сверху/снизу (по ~104 пикселя)
    - Преимущество: минимальные изменения кода
    - Недостаток: не используется весь экран
  - **Вариант Г: Апскейл квадратного контента + область управления (✅ РЕКОМЕНДУЕТСЯ)**
    - Масштабировать квадратный контент 240×240 до максимального размера, вписывающегося в ширину экрана
    - Расчет: масштаб = 368/240 = 1.533, размер после масштабирования = 368×368
    - Разместить масштабированный контент в верхней части экрана (координаты 0,0 - 368,368)
    - Использовать нижнюю область (368×80 пикселей, координаты 0,368 - 368,448) для управляющих элементов
    - Преимущества:
      - Минимальные изменения в коде основного контента (только масштабирование при выводе)
      - Родные спрайты остаются в исходном разрешении 240×240
      - Использование всего экрана эффективно
      - Дополнительное пространство для UI элементов управления (виртуальные кнопки, статус-бары)
      - Легко реализовать через библиотеку дисплея или вручную
    - Реализация:
      - Создать фреймбуфер 240×240 для основного контента (как в текущем коде)
      - Масштабировать и выводить в область 0,0 - 368,368
      - Рисовать управляющие элементы отдельно в области 0,368 - 368,448

#### Проблема 2: Другая библиотека дисплея
- **Проблема:** TFT_eSPI может не поддерживать AMOLED дисплеи
- **Влияние:**
  - Все вызовы `TFT_eSPI` и `TFT_eSprite` могут не работать
  - Настройки пинов в `User_Setup.h` могут быть неактуальны
- **Решение:**
  - Найти подходящую библиотеку для AMOLED (возможно, библиотеки LilyGO)
  - Адаптировать или переписать код работы с дисплеем
  - Обновить конфигурацию пинов

#### Проблема 3: Соотношение сторон (квадрат → вертикальный прямоугольник)
- **Проблема:** 
  - Текущее: 240×240 = 1:1 (квадрат)
  - Целевое: 368×448 = ~0.82:1 (вертикальный, высота больше ширины на ~22%)
- **Влияние:**
  - UI рассчитан на квадратный формат
  - При переносе на вертикальный экран элементы могут быть растянуты или сжаты
  - Спрайты питомца (115×110) могут выглядеть иначе
  - Навигация и меню требуют адаптации
- **Решение (выбранный подход):**
  - **Апскейл квадратного контента + область управления:**
    - Основной контент (240×240) масштабируется до 368×368 (масштаб 1.533×)
    - Размещается в верхней части экрана (координаты 0,0 - 368,368)
    - Нижняя область 368×80 пикселей используется для управляющих элементов
    - Преимущества:
      - Родные спрайты остаются в исходном разрешении 240×240
      - Масштабирование происходит только при выводе на экран
      - Минимальные изменения в коде рендеринга
      - Дополнительное пространство для виртуальных кнопок и UI элементов
    - Реализация:
      - Фреймбуфер остается 240×240 (как в текущем коде)
      - При выводе на экран: масштабировать через библиотеку дисплея или вручную
      - Управляющие элементы рисуются отдельно в нижней области

---

## 2. Отсутствие светодиодов (NeoPixels)

### Текущий код

```cpp
// TamaFi.ino
#include <Adafruit_NeoPixel.h>

#define LED_PIN    1
#define LED_COUNT  4

Adafruit_NeoPixel leds(LED_COUNT, LED_PIN, NEO_GRB + NEO_KHZ800);

// Функции управления светодиодами:
void ledsOff()
void applyLedBrightness()
void ledsHappy()    // Фиолетовый цвет
void ledsSad()      // Красный цвет
void ledsWifi()     // Синий цвет
void ledsRest()     // Темно-синий цвет
void startupBreathing(uint8_t r, uint8_t g, uint8_t b)

// Использование в коде:
- При успешной/неуспешной охоте (sndGoodFeed/sndBadFeed)
- При обнаружении Wi‑Fi (sndDiscover)
- При начале/окончании отдыха (sndRestStart/sndRestEnd)
- При смерти питомца
- При запуске устройства (startupBreathing)
- В режиме отдыха (дыхание синим цветом)
```

**Функциональность светодиодов:**
- **Визуальная обратная связь:** Показывают настроение питомца (счастливый/грустный)
- **Индикация активности:** Синий цвет при сканировании Wi‑Fi
- **Индикация состояния:** Темно-синий при отдыхе
- **Настройки:** Яркость регулируется (Low/Mid/High: 20/90/180)
- **Управление:** Можно включить/выключить через меню Controls

### Целевое устройство

**Характеристики:**
- **Светодиоды:** Отсутствуют
- **Альтернатива:** Только AMOLED дисплей

### Проблемы и решения

#### Проблема 1: Потеря визуальной обратной связи
- **Проблема:** Все функции светодиодов не будут работать
- **Влияние:**
  - Нет визуальной индикации настроения питомца
  - Нет индикации активности (охота, отдых)
  - Потеря обратной связи при событиях
- **Решение:**
  - **Вариант А:** Использовать экран для визуальной обратной связи
    - Добавить цветные индикаторы на экране (полоски, рамки, иконки)
    - Изменять цвет фона или элементов UI в зависимости от состояния
    - Добавить анимации на экране вместо светодиодов
  - **Вариант Б:** Удалить функциональность светодиодов
    - Убрать все вызовы функций светодиодов
    - Удалить настройки яркости светодиодов из меню
    - Оставить только звуковую обратную связь

#### Проблема 2: Код управления светодиодами
- **Проблема:** Весь код работы со светодиодами станет неактуальным
- **Влияние:**
  - Компиляция может падать из-за отсутствия библиотеки `Adafruit_NeoPixel`
  - Занимает память и ресурсы процессора
- **Решение:**
  - Удалить `#include <Adafruit_NeoPixel.h>`
  - Удалить объект `leds` и все связанные функции
  - Удалить переменную `neoPixelsEnabled` или использовать для управления экранными индикаторами
  - Удалить настройку яркости светодиодов из меню Controls
  - Заменить вызовы функций светодиодов на функции отрисовки на экране

#### Проблема 3: Настройки в меню
- **Проблема:** В меню Controls есть пункт "NeoPixels" для включения/выключения
- **Влияние:** Пункт меню станет бесполезным
- **Решение:**
  - Удалить пункт "NeoPixels" из меню Controls
  - Или переименовать в "Screen Indicators" и использовать для управления экранными индикаторами

---

## 3. Отсутствие физических кнопок (замена на тач-экран)

### Текущий код

```cpp
// TamaFi.ino
// --------- Hardware pins ---------
#define BTN_UP    13
#define BTN_OK    12
#define BTN_DOWN  11

#define BTN_RIGHT1 8
#define BTN_RIGHT2 9
#define BTN_RIGHT3 10

// Состояния кнопок
bool lastUp   = HIGH;
bool lastOk   = HIGH;
bool lastDown = HIGH;
bool lastR1 = HIGH;
bool lastR2 = HIGH;
bool lastR3 = HIGH;

// Функция обработки нажатий
bool buttonPressed(int pin, bool &lastState) {
  bool s = digitalRead(pin);
  bool p = (lastState == HIGH && s == LOW);
  lastState = s;
  return p;
}

// Инициализация в setup()
pinMode(BTN_UP,   INPUT_PULLUP);
pinMode(BTN_OK,   INPUT_PULLUP);
pinMode(BTN_DOWN, INPUT_PULLUP);
pinMode(BTN_RIGHT1, INPUT_PULLUP);
pinMode(BTN_RIGHT2, INPUT_PULLUP);
pinMode(BTN_RIGHT3, INPUT_PULLUP);
```

**Использование кнопок:**

#### Левая группа (3 кнопки):
- **BTN_UP (13):** Навигация вверх в меню
- **BTN_OK (12):** Подтверждение выбора, вход в меню
- **BTN_DOWN (11):** Навигация вниз в меню

#### Правая группа (3 кнопки):
- **BTN_RIGHT1 (8):** Быстрый доступ к Pet Status с главного экрана
- **BTN_RIGHT2 (9):** Быстрый доступ к Environment с главного экрана
- **BTN_RIGHT3 (10):** Быстрый доступ к Diagnostics с главного экрана

**Логика обработки:**
- На главном экране: OK → открыть меню, R1/R2/R3 → быстрый доступ к экранам
- В меню: UP/DOWN → навигация, OK → выбор пункта
- На информационных экранах: OK → возврат в меню
- На экране Boot: любая кнопка → переход к главному экрану

### Целевое устройство

**Характеристики:**
- **Физические кнопки:** 2 кнопки (расположение и назначение требуют уточнения)
- **Тач-экран:** Емкостный, покрывает всю поверхность дисплея (1.8")
- **Возможности тач-экрана:**
  - Тап (одиночное касание)
  - Долгий тап (длительное удержание)
  - Свайп (вертикальный/горизонтальный)

### Проблемы и решения

#### Проблема 1: Недостаточно физических кнопок
- **Проблема:** Только 2 физические кнопки вместо 6
- **Влияние:**
  - Невозможно реализовать всю навигацию только физическими кнопками
  - Быстрые клавиши (R1/R2/R3) не могут быть реализованы физически
- **Решение:**
  - Использовать тач-экран для эмуляции недостающих кнопок
  - Создать виртуальные кнопки на экране
  - Использовать жесты для навигации

#### Проблема 2: Замена логики обработки ввода
- **Проблема:** Весь код `handleButtons()` использует `digitalRead()` для физических кнопок
- **Влияние:**
  - Код не будет работать с тач-экраном
  - Нужна полная переработка системы ввода
- **Решение:**
  - **Вариант А:** Создать абстрактный слой ввода
    ```cpp
    // Новый модуль input.h/input.cpp
    enum InputEvent {
      INPUT_UP,
      INPUT_DOWN,
      INPUT_OK,
      INPUT_R1,
      INPUT_R2,
      INPUT_R3
    };
    
    bool getInputEvent(InputEvent event);  // Абстракция для кнопок/тача
    ```
  - **Вариант Б:** Переписать `handleButtons()` для работы с тач-экраном
    - Обрабатывать касания тач-экрана
    - Маппить зоны касания на действия кнопок
    - Обрабатывать жесты (свайпы)

#### Проблема 3: Эмуляция 6 кнопок через тач-экран
- **Проблема:** Нужно заменить 6 физических кнопок тач-управлением
- **Влияние:** Требуется дизайн виртуальных кнопок или жестов
- **Решение (выбранный подход):**
  - **Виртуальные кнопки в области управления (368×80)**
    - Разместить виртуальные кнопки в нижней области экрана (y: 368-448)
    - Левая зона (~120×80): кнопки UP, OK, DOWN для навигации
    - Правая зона (~120×80): кнопки R1, R2, R3 для быстрых действий
    - Центральная зона (~128×80): статус-бары или дополнительная информация
    - Минимальный размер кнопок: 44×44 пикселя для удобного нажатия
    - Визуальная обратная связь: подсветка при нажатии, анимации
  - **Альтернативные варианты:**
    - **Жесты:** Свайп вверх/вниз → UP/DOWN, тап по центру → OK
    - **Комбинированный:** 2 физические кнопки + виртуальные в области управления

#### Проблема 4: Быстрые клавиши (R1/R2/R3)
- **Проблема:** На главном экране R1/R2/R3 обеспечивают быстрый доступ к экранам
- **Влияние:** Нужна альтернатива для тач-экрана
- **Решение:**
  - Добавить виртуальные кнопки на главном экране
  - Использовать свайпы в разные стороны для быстрого доступа
  - Добавить иконки в углах экрана для тапа

#### Проблема 5: Навигация в меню
- **Проблема:** UP/DOWN кнопки используются для навигации по спискам
- **Влияние:** Нужна альтернатива через тач
- **Решение:**
  - Вертикальный свайп для прокрутки списка
  - Тап по пункту меню для выбора
  - Виртуальные кнопки UP/DOWN на экране

---

## 4. Реализация апскейла и области управления

### Архитектура экрана

```
┌─────────────────────────────────────┐
│                                     │
│   Основной контент (368×368)        │
│   Масштабированный из 240×240       │
│   Масштаб: 1.533×                   │
│                                     │
│   [Питомец, фон, эффекты]           │
│                                     │
├─────────────────────────────────────┤
│   Область управления (368×80)       │
│   Виртуальные кнопки, статус-бары    │
│   Тач-зоны для навигации             │
└─────────────────────────────────────┘
```

### Технические детали реализации

#### Масштабирование контента

**Параметры:**
- Исходный фреймбуфер: 240×240 пикселей (остается без изменений)
- Масштаб: 368/240 = 1.533× (по ширине и высоте)
- Результирующий размер: 368×368 пикселей
- Область вывода: координаты (0, 0) - (368, 368)

**Методы масштабирования:**
1. **Встроенные функции библиотеки дисплея** (предпочтительно)
   - Если библиотека поддерживает масштабирование спрайтов
   - Использовать функции типа `pushScaled()` или аналогичные
2. **Билинейная интерполяция** (качество)
   - Плавное масштабирование с сглаживанием
   - Требует больше вычислений
3. **Ближайший сосед (nearest neighbor)** (производительность)
   - Быстрое масштабирование без сглаживания
   - Может давать пикселизацию, но быстрее

#### Область управления (368×80)

**Расположение:** Нижняя часть экрана (y: 368-448)

**Предлагаемое разделение на зоны:**

```
┌──────────┬──────────────┬──────────┐
│   Левая  │   Центральная│  Правая  │
│  зона    │     зона     │   зона   │
│          │              │          │
│ UP/OK/   │  Статус-бары │  R1/R2/  │
│ DOWN     │  или доп.    │  R3 или  │
│ (навигация)│ информация │  меню    │
└──────────┴──────────────┴──────────┘
```

**Размеры зон (пример):**
- Левая зона: ~120×80 (виртуальные кнопки навигации)
- Центральная зона: ~128×80 (статус-бары, информация)
- Правая зона: ~120×80 (быстрые действия)

**Минимальный размер тач-элементов:** 44×44 пикселя (рекомендация для удобного нажатия)

#### Кодовая структура

```cpp
// Новые константы для целевого устройства
#define TARGET_W 368
#define TARGET_H 448
#define CONTENT_AREA_H 368  // Высота области контента
#define CONTROL_AREA_H 80   // Высота области управления
#define SCALE_FACTOR 1.533f // Коэффициент масштабирования

// Функция вывода на целевое устройство
void renderToTarget() {
  // 1. Отрисовать основной контент в фреймбуфер 240×240 (как сейчас)
  uiDrawScreen(currentScreen, mainMenuIndex, controlsIndex, settingsMenuIndex);
  
  // 2. Масштабировать и вывести в область 0,0 - 368,368
  scaleAndDraw(fb, 0, 0, 368, 368, SCALE_FACTOR);
  
  // 3. Отрисовать управляющие элементы в области 0,368 - 368,448
  drawControlArea(0, 368, 368, CONTROL_AREA_H);
}

// Функция отрисовки области управления
void drawControlArea(int x, int y, int w, int h) {
  // Виртуальные кнопки для навигации
  drawVirtualButton(x + 20, y + 10, 40, 40, "UP");
  drawVirtualButton(x + 20, y + 30, 40, 40, "OK");
  drawVirtualButton(x + 20, y + 50, 40, 40, "DN");
  
  // Статус-бары или дополнительная информация
  // ...
  
  // Быстрые действия (R1/R2/R3)
  // ...
}
```

### Преимущества подхода

1. **Минимальные изменения в коде:**
   - Основной код рендеринга остается без изменений
   - Фреймбуфер 240×240 работает как раньше
   - Масштабирование происходит только при выводе

2. **Сохранение качества спрайтов:**
   - Родные спрайты остаются в исходном разрешении
   - Масштабирование происходит программно при выводе
   - Возможность использовать разные алгоритмы масштабирования

3. **Эффективное использование экрана:**
   - Весь экран используется (нет черных полос)
   - Дополнительное пространство для UI элементов
   - Улучшенный UX с виртуальными кнопками

4. **Гибкость:**
   - Легко изменить размер области управления
   - Можно адаптировать layout под разные экраны
   - Возможность скрывать/показывать область управления

---

## 5. Дополнительные различия

### Библиотеки

#### Текущий код:
```cpp
#include <TFT_eSPI.h>           // Для TFT дисплея
#include <Adafruit_NeoPixel.h>  // Для светодиодов
```

#### Целевое устройство:
- **GFX_Library_for_Arduino:** v1.4.9 (для SH8601 AMOLED дисплея)
  - Драйвер: Arduino_SH8601
  - Интерфейс: QSPI (Arduino_ESP32QSPI)
- **Arduino_DriveBus:** Для FT3168 тач-контроллера (I2C)
- **ESP32_IO_Expander:** v0.0.3 (для TCA9554 GPIO expander)
- **Adafruit_NeoPixel:** Не нужна (нет светодиодов)
- **TFT_eSPI:** Не используется (заменена на GFX_Library_for_Arduino)

### Пины (Pinout)

#### Текущий код использует:
- Пины 8, 9, 10, 11, 12, 13 — для кнопок
- Пин 1 — для светодиодов
- Пины для дисплея (MOSI, SCLK, CS, DC, RST) — в User_Setup.h

#### Целевое устройство:
- **Кнопки:** 
  - BOOT: GPIO0 (низкий уровень = нажата)
  - PWR: EXIO4 через TCA9554 (высокий уровень = нажата)
- **Светодиоды:** Нет
- **Тач-экран:** FT3168 через I2C (IIC_SDA, IIC_SCL - определяются в Mylibrary)
- **Дисплей:** SH8601 через QSPI (LCD_CS, LCD_SCLK, LCD_SDIO0-3 - определяются в Mylibrary)
- **GPIO Expander:** TCA9554 через I2C (для дополнительных пинов)

### Энергопотребление

#### Текущий код:
- Управление яркостью TFT через PWM (TFT_BRIGHTNESS_PIN 7)
- Управление яркостью светодиодов (3 уровня)

#### Целевое устройство:
- **AMOLED:** Экономит энергию на черных пикселях
- **Рекомендация:** Использовать темные темы UI
- **Тач-экран:** Потребляет дополнительную энергию при активном сканировании

---

## Сводная таблица различий

| Компонент | Текущий код | Целевое устройство | Стратегия адаптации | Критичность |
|-----------|-------------|-------------------|---------------------|-------------|
| **Разрешение экрана** | 240×240 (1:1) | 368×448 (~0.82:1) | Апскейл 240×240 → 368×368 + область управления 368×80 | 🔴 Высокая |
| **Тип дисплея** | TFT ST7789 | AMOLED | Замена библиотеки дисплея, использование темных тем | 🔴 Высокая |
| **Библиотека дисплея** | TFT_eSPI | GFX_Library_for_Arduino (v1.4.9) | Замена на GFX_Library_for_Arduino с драйвером Arduino_SH8601 | 🔴 Высокая |
| **Физические кнопки** | 6 кнопок | 2 кнопки | Виртуальные кнопки в области управления 368×80 + жесты | 🔴 Высокая |
| **Тач-экран** | Нет | FT3168 (I2C) | Библиотека Arduino_DriveBus для FT3168 | 🔴 Высокая |
| **Светодиоды** | 4× WS2812-2020 | Нет | Замена на экранные индикаторы в области управления | 🟡 Средняя |
| **Библиотека светодиодов** | Adafruit_NeoPixel | Не нужна | Удалить библиотеку и связанный код | 🟡 Средняя |
| **Управление яркостью** | PWM для TFT + LEDs | Только для AMOLED | Удалить управление яркостью светодиодов, оставить только для дисплея | 🟡 Средняя |

---

## Приоритеты миграции

### Критичные изменения (блокируют работу):
1. ✅ Адаптация разрешения экрана (240×240 → 368×448)
2. ✅ Адаптация соотношения сторон (квадрат → вертикальный прямоугольник)
3. ✅ Замена библиотеки дисплея (если TFT_eSPI не поддерживает AMOLED)
4. ✅ Реализация тач-управления вместо кнопок
5. ✅ Обновление конфигурации пинов

### Важные изменения (влияют на функциональность):
5. ✅ Удаление/замена функциональности светодиодов
6. ✅ Адаптация UI под новое разрешение
7. ✅ Реализация виртуальных кнопок или жестов

### Опциональные улучшения:
8. ⚪ Оптимизация под AMOLED (темные темы)
9. ⚪ Улучшение UX для тач-экрана
10. ⚪ Использование преимуществ AMOLED (глубокий черный, высокая контрастность)

---

## Рекомендации по миграции

1. **Начать с исследования:**
   - Определить точное разрешение AMOLED дисплея
   - Найти подходящую библиотеку для дисплея
   - Выяснить распиновку кнопок и тач-экрана

2. **Создать абстракцию ввода:**
   - Вынести обработку ввода в отдельный модуль
   - Поддержать и физические кнопки, и тач-экран
   - Облегчить тестирование и отладку

3. **Адаптировать UI постепенно:**
   - **Этап 1:** Реализовать апскейл 240×240 → 368×368 с выводом в верхнюю область экрана
   - **Этап 2:** Добавить нижнюю область управления (368×80) для виртуальных кнопок
   - **Этап 3:** Адаптировать виртуальные кнопки под тач-экран
   - **Этап 4:** Оптимизировать layout управляющих элементов
   - **Этап 5:** Добавить улучшения для тач-экрана (жесты, анимации)

4. **Заменить светодиоды экранными индикаторами:**
   - Сохранить функциональность визуальной обратной связи
   - Использовать преимущества экрана (больше информации)

---

*Дата создания: 23 января 2026*
*Статус: Планирование миграции*
